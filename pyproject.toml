[project]
name = "hospital-management"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "yogeshyadla15-ux", email = "yogeshyadla15@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.141.1",
    "sqlalchemy>=2.0.54",
    "uvicorn>=0.53.0",
]

[project.scripts]
hospital-management = "hospital_management:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
